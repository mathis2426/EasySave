<Page x:Class="WPFApp.Settings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Settings">

    <Grid Background="White">
        <ListView x:Name="ExtensionsList" ItemsSource="{Binding Extensions}" SelectedItem="{Binding SelectedExtension}" Margin="50,50,0,0" Width="250" Height="190" HorizontalAlignment="Left" VerticalAlignment="Top">
        <!-- Liste des extensions normales -->
        <ListView x:Name="ExtensionsList" ItemsSource="{Binding Extensions}" SelectedItem="{Binding SelectedExtension}" Margin="50,50,547,150">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{Binding ExtensionsName}" Width="250" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="ExtensionsName" Width="200" DisplayMemberBinding="{Binding Name}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBox Text="{Binding NewExtensionName, UpdateSourceTrigger=PropertyChanged}" Margin="50,264,0,0" Height="28" Width="120" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Button Content="{Binding Add}" Command="{Binding AddExtensionCommand}" Margin="175,264,0,0" Height="28" HorizontalAlignment="Left" Width="61" VerticalAlignment="Top"/>
        <TextBox Text="{Binding OutputText}" IsReadOnly="True" Margin="50,335,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="517" Height="53" HorizontalAlignment="Left" />
        <ComboBox  ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding SelectedLanguage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Margin="632,20,0,0" VerticalAlignment="Top" Width="120"/>
        <Button x:Name="ExitCreateJob" Content="{Binding Exit}" HorizontalAlignment="Left" Margin="626,356,0,0" VerticalAlignment="Top" Height="32" Width="133"/>
        <Button Content="{Binding Delete}" Command="{Binding DeleteExtensionCommand}" Margin="241,264,0,0" Width="59" Height="28" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Label Content="{Binding BlockingApplication}" HorizontalAlignment="Left" Margin="376,50,0,0" VerticalAlignment="Top"/>
        <TextBox Text="{Binding BlockingApp, UpdateSourceTrigger=LostFocus}" HorizontalAlignment="Left" Margin="379,80,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <Label Content="{Binding Language}" HorizontalAlignment="Left" Margin="568,18,0,0" VerticalAlignment="Top"/>

        <!-- Nouvelle liste : extensions prioritaires (à droite) -->
        <ListView x:Name="PriorityExtensionsList" ItemsSource="{Binding PriorityExtensions}" SelectedItem="{Binding SelectedPriorityExtension}" Margin="270,50,341,150">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="PriorityExtension" Width="200" DisplayMemberBinding="{Binding Name}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Champ pour ajouter une extension -->
        <TextBox Text="{Binding NewExtensionName, UpdateSourceTrigger=PropertyChanged}" Margin="479,133,152,289" Width="169" Height="28"/>
        <Button Content="Add Extension" Command="{Binding AddExtensionCommand}" Margin="678,133,24,289" Width="98" Height="28" Click="Button_Click"/>
        <Button Content="Delete Extension" Command="{Binding DeleteExtensionCommand}" Margin="678,175,24,247" Width="98" Height="28"/>

        <!-- Champ pour ajouter une extension prioritaire -->
        <TextBox Text="{Binding NewPriorityExtension, UpdateSourceTrigger=PropertyChanged}" Margin="479,242,152,180" Width="169" Height="28"/>
        <Button Content="Add Priority" Command="{Binding AddPriorityExtensionCommand}" Margin="678,242,24,180" Width="98" Height="28"/>
        <Button Content="Delete Priority" Command="{Binding DeletePriorityExtensionCommand}" Margin="678,287,24,135" Width="98" Height="28"/>

        <!-- Output text -->
        <TextBox Text="{Binding OutputText}" IsReadOnly="True" Margin="50,335,233,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="517" Height="53"/>

        <!-- Combobox and label -->
        <ComboBox HorizontalAlignment="Left" Margin="632,20,0,0" VerticalAlignment="Top" Width="120"/>
        <TextBox HorizontalAlignment="Left" Margin="555,20,0,0" TextWrapping="Wrap" Text="Language" VerticalAlignment="Top" Width="72" Height="20"/>

        <!-- Exit Button -->
        <Button x:Name="ExitCreateJob" Content="Exit" HorizontalAlignment="Left" Margin="643,356,0,0" VerticalAlignment="Top" Height="32" Width="133" Click="ExitCreateJob_Click"/>
    </Grid>
</Page>
