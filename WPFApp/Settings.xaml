<Page x:Class="WPFApp.Settings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Settings">

    <Grid Background="White">
        <ListView x:Name="ExtensionsList" ItemsSource="{Binding Extensions}" SelectedItem="{Binding SelectedExtension}" Margin="50,50,500,150">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ExtensionsName" Width="250" DisplayMemberBinding="{Binding Name}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBox Text="{Binding NewExtensionName, UpdateSourceTrigger=PropertyChanged}" Margin="479,197,152,225" Width="169" Height="28" TextChanged="TextBox_TextChanged"/>
        <Button Content="Add Extension" Command="{Binding AddExtensionCommand}" Margin="678,197,24,225" Width="98" Height="28"/>
        <TextBox Text="{Binding OutputText}" IsReadOnly="True" Margin="50,335,233,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="517" Height="53"/>
        <ComboBox HorizontalAlignment="Left" Margin="632,20,0,0" VerticalAlignment="Top" Width="120"/>
        <TextBox HorizontalAlignment="Left" Margin="555,20,0,0" TextWrapping="Wrap" Text="Language" VerticalAlignment="Top" Width="72" Height="20"/>
        <Button x:Name="ExitCreateJob" Content="Exit" HorizontalAlignment="Left" Margin="643,356,0,0" VerticalAlignment="Top" Height="32" Width="133"/>
        <Button Content="Delete Extension" Command="{Binding DeleteExtensionCommand}" Margin="678,242,24,180" Width="98" Height="28"/>
    </Grid>
</Page>
