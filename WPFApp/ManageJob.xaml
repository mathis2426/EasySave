<Page x:Class="WPFApp.ManageJob"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ManageJob">

    <Grid RenderTransformOrigin="0.481,0.464" Background="White">
        <Button Content="{Binding StartJobLabel}" Command="{Binding StartCommand}" HorizontalAlignment="Left" Margin="37,269,0,0" VerticalAlignment="Top" Height="40" Width="114"/>
        <Button Content="{Binding PauseJobLabel}" Command="{Binding StopCommand}" HorizontalAlignment="Left" Margin="440,269,0,0" VerticalAlignment="Top" Height="40" Width="114" RenderTransformOrigin="0.886,-0.4"/>
        <Button Content="{Binding ResumeJobLabel}" Command="{Binding ResumeCommand}" HorizontalAlignment="Left" Margin="238,269,0,0" VerticalAlignment="Top" Height="40" Width="115"/>
        <Button x:Name="ExitManageJob" Content="{Binding ExitLabel}" HorizontalAlignment="Left" Margin="619,356,0,0" VerticalAlignment="Top" Height="32" Width="133" Click="ButtonLeave_ClickManageJob"/>
        <TextBox x:Name="outputBox" Text="{Binding OutputString}" HorizontalAlignment="Left" Margin="37,345,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="517" Height="53"/>
        <Label Content="{Binding PercentageCompleted}" VerticalAlignment="Top" HorizontalAlignment="Left" Height="27" Margin="37,61,0,0"/>
        <ProgressBar HorizontalAlignment="Left" Value="{Binding ProgressValue}" Minimum="0" Maximum="100" Height="42" Margin="220,55,0,0" VerticalAlignment="Top" Width="322"/>
        <TextBox HorizontalAlignment="Left" Text="{Binding InputFileString}" Margin="220,112,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="322"/>
        <TextBox HorizontalAlignment="Left" Text="{Binding OutputFileString}" Margin="220,142,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="322"/>
        <Rectangle HorizontalAlignment="Center" Height="450" Stroke="White" VerticalAlignment="Center" Width="800" Opacity="100" Fill="White" Panel.ZIndex="-1"/>
        <Label Content="{Binding InputFileLabel}" HorizontalAlignment="Left" Height="27" Margin="37,107,0,0" VerticalAlignment="Top" Width="140"/>
        <Label Content="{Binding OutputFileLabel}" HorizontalAlignment="Left" Height="27" Margin="37,137,0,0" VerticalAlignment="Top" Width="140"/>
        <Label Content="{Binding PriorityFileLabel}" HorizontalAlignment="Left" Height="27" Margin="37,176,0,0" VerticalAlignment="Top" Width="140"/>
        <Label Content="{Binding JobNameLabel}" HorizontalAlignment="Left" Margin="37,22,0,0" VerticalAlignment="Top" Width="92"/>
        <Label Content="{Binding JobName}" HorizontalAlignment="Left" Margin="137,22,0,0" VerticalAlignment="Top"/>
        <Label Content="{Binding JobTypeLabel}" HorizontalAlignment="Left" Margin="41,217,0,0" VerticalAlignment="Top"/>
        <Label Content="{Binding JobType}" HorizontalAlignment="Left" Margin="128,217,0,0" VerticalAlignment="Top"/>

    </Grid>
</Page>
